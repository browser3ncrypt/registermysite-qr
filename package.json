{
  "name": "registermysiteQR",
  "version": "1.1.0",
  "private": true,
  "description": "QR code generator + password-protected link shortener with analytics",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250422.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.14.0"
  }
}
